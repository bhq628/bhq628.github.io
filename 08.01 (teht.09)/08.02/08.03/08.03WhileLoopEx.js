// TODO: JavaScript code
// Confirm = true. Cancel = false.
function showResults() {
    
    
    while (confirm("Do you really want to exit?")) {
        alert("An early bird gets the worm!");
    }
}

showResults();